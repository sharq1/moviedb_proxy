{
  "name": "moviedb_proxy",
  "version": "0.0.1",
  "description": "Proxy leveraging MovieDB for searching movies",
  "main": "index.html",
  "scripts": {
    "start": "http-server",
    "test": "jest --config jest.config.json",
    "test:watch": "npm run test -- -i --watch"
  },
  "devDependencies": {
    "documentation": "^4.0.0-rc.1",
    "jest": "^20.0.4"
  },
  "dependencies": {
    "http-server": "^0.10.0"
  }
}
